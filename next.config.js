/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = nextConfig

module.exports = {
  images: {
    domains: ["external-content.duckduckgo.com"], // Agrega el dominio de tu imagen remota aquí
  },
};
